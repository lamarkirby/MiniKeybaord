#ifndef SECRETS_H
#define SECRETS_H

/**
 * @file secrets.h.example
 * @brief Template for WiFi and API credentials
 *
 * Copy this file to secrets.h and fill in your actual credentials.
 * The secrets.h file is gitignored to prevent accidental commits.
 *
 * SECURITY WARNING:
 * - Never commit secrets.h to version control
 * - Use a strong, unique API key
 * - Rotate API key periodically
 * - Don't share your API key
 */

// WiFi Credentials
#define WIFI_SSID "YourNetworkName"
#define WIFI_PASSWORD "YourWiFiPassword"

// API Security
// Generate a strong random key with: openssl rand -hex 32
#define API_KEY "change-me-to-a-secure-random-key"

#endif
